<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <application
        android:name=".CreaturemonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mvp.MainActivityMvp"
            android:label="@string/mvi_demo" />
        <activity android:name=".mvi.activities.MainActivityMvi"
            android:label="@string/mvi_demo"/>
        <activity android:name=".mvvm.activities.MainActivityMvvm"
            android:label="@string/mvvm_demo"/>

        <activity android:name=".mvp.view.allcreatures.AllCreaturesActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/all_creatures"/>

        <activity android:name=".mvp.view.creature.CreatureActivity"
            android:label="@string/all_creatures"/>
    </application>

</manifest>